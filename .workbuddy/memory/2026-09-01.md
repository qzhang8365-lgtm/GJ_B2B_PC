# 2026-09-01

## 日报自动化调整：禁止写文件 + 用户希望在当前对话内运行
- 用户要求：以后定时任务跑时不要重新开一个任务对话，直接在当前对话里工作。
- 技术现实：WorkBuddy 定时任务由系统调度，每次默认在独立会话运行；"是否开新会话"不在 automation_update 配置项内，无法锁定到指定对话。
- 已做：把两条每日自动化（automation-1787031464247、automation-1787047359217）的提示词改为「只通过企业微信发消息，禁止创建/写入任何本地文件、禁止生成 artifact」，避免每次运行往项目根目录撒 工作日报_*.md / 日报草稿_*.md。
- 项目根目录仍留有 08-18~08-28 的 工作日报_*.md 与 日报草稿_*.md（旧自动运行产物），待用户决定是否清理。
- 替代方案（若用户坚持"在当前对话里"）：每天在本对话说"跑日报"，我当场生成并企微发送（如 08-18 手动跑那样），完全在对话内进行。

## 用户确认：日报"填表"维持手动粘贴，不尝试桌面/浏览器自动化
- 用户问能否控制桌面打开企业微信自动填 journal。结论：无远程桌面/GUI 控制能力；journal 类型也无 CLI 接口可写。
- 用户明确选择「维持手动粘贴」：AI 每天 17:30 生成三段草稿发企微 → 用户自己贴进《科研-综合产品日报》字段并提交。不加载 agent-browser 尝试填网页版 journal。
- 备选（未选）：① 浏览器自动化半自动填网页版（需登录态、selector 不稳）；② 改成普通 doc 类型 + 开通文档权限实现真自动写入（不再用 journal 模板）。
