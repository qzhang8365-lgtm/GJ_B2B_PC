---
description: 用户调整组件规则时必须先改组件规范契约，不能只改模式页或预览。
alwaysApply: true
---

# 组件意见先落规范

用户提出组件修改意见、视觉纠正或规则调整时，必须同步写进该组件的规范层，禁止只改 `preview/patterns/` 或页面私有 CSS。

至少更新（已有文件才改，不编造 Figma 值）：

- `references/components/<name>/rules.md`
- `references/components/<name>/schema.json`（sizes / constraints / confirmedDecisions）
- 若影响属性或实现映射：同目录 `mapping.json`、`audit.md`
- 若影响尺寸或状态：`references/tokens/components/<name>.tokens.json`，再跑 token 构建
- 共享实现：`assets/styles/gj-b2b-components.css` 的 `gj-*` 基座
- 仅当意见属于某页面模式组合时，再改 `references/page-patterns.md`

不要把组件规则只写进 `SKILL.md` 或只留在模式页。模式页只引用共享基座。

示例：Steps 全宽两侧 48px、指示器与文字居中 → 写入 Steps 的 rules / schema / mapping，并改 `.gj-steps`；不是只改 `step-task.html`。
