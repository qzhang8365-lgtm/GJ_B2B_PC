# Icon 图标

> 从 `references/design-system-rules.md` 拆分（2026-09-08）。跨组件的颜色/字体/布局/圆角等基础规范见该文件“基础规范”章节。

#### 资源类型

- General：无渐变、无动效的普通线性或填充图标，采用 Icon Font 或已上传的普通 SVG 资源调用。清单为 `icons/manifest.json`。
- Effect Icon / Glass Icon：包含渐变、毛玻璃或复杂视觉效果的图标，以独立 SVG 维护；页面 `<img>` 必须使用配套 PNG。清单为 `icons/glass-manifest.json`。
- File Icon：仅表示文件类型（上传列表、附件）。清单为 `icons/file-manifest.json`。
- Motion Icon：Lottie JSON 动效图标。清单为 `icons/motion-manifest.json`。
- 查找时先读 `icons/index.json` 判断类型，再打开对应清单。不把复杂 Effect Icon 转换成单色 Icon Font，以免丢失渐变、透明度和细节。

#### 命名规则

- 标准命名格式为 `icf_图标分类_图标名称`。
- 全部名称使用标准英文，不使用拼音，避免同音重复和搜索歧义。
- 同一图标的普通与填充版本使用相同基础名称，填充版追加 `-fill`。
- `icf_` 与分类之间使用下划线分段；图标名称内部的多个英文单词统一用连字符连接，不再混用下划线。例如使用 `icf_system_delete-back-fill`，不使用 `icf_system_delete_back-fill`。
- 旧名称只能登记为兼容别名：运行时先解析到规范名，资源清单、搜索结果、复制名称和新代码始终输出规范名；别名不得生成第二条资源或造成图标重复展示。
- 图标同时维护可搜索关键词；搜索名称应包含常见英文功能词，例如 close、search、home。
- 2026-09-10 ICO-004：20 个带数字后缀的 System 图标（`loading1` / `loading2`、`settings1` / `settings2` 等）**暂不**改成语义英文名，也**不**加兼容别名。调用继续用现名（如 `icf_system_loading1`），不要自行改名或发明 alias。

#### 尺寸与绘制规则

- 默认画布为 24×24px，标准描边 2px并轮廓化。
- 圆角和边缘原则上使用 2px 半径，保持角度和视觉重心一致。
- 标准尺寸梯度：`12 / 16 / 20 / 24 / 28 / 32 / 36 ...`。
- 静态组件基座中，General Icon 使用 `gj-icon`，默认 24px，尺寸档为 `gj-icon-12 / -16 / -20 / -24 / -28 / -32 / -36`。资源地址写入 `--gj-icon`，颜色继承 `currentColor`，行内与文字并排时垂直居中。Effect Icon 与 Motion Icon 保持独立文件，不得套进 `gj-icon`。
- 图标根据相邻文字和组件尺寸缩放，优先使用 4px 梯度，不随意使用中间尺寸。
- 图标视觉尺寸应与画布和相邻文字协调；不能仅把 SVG 外框设为目标尺寸而忽略内部图形偏移。

#### 颜色与组合

- 普通图标默认跟随相邻文字颜色。例如按钮图标必须跟随按钮文字的 Default、Hover、Pressed、Disabled 和 Loading 状态色。
- General Icon 原则上保持单色，通过语义颜色表达状态；不得仅因需要不同颜色而把普通图标随意转换为 Effect Icon。
- 图标用于表达独立状态时，才使用对应的 Feedback 或业务语义色。
- Effect Icon 保留资源本身的渐变和复杂色彩，不强制继承文字颜色。
- 图标与文字的间距由所在组件规则决定，不建立一个全局固定间距。

#### 资源使用边界

- 页面和组件只能引用已上传图标库资源，不使用 Emoji、Unicode 字符、CSS 临时绘制或近似图标替代。
- AI 应先在图标库中寻找业务语义接近且不会造成误解的图标；允许复用语义相近资源，但不得只因外形相似而使用错误含义的图标。没有可靠资源时应报告缺失项或提出新增定制图标。
- 普通版与 Fill 版在资源展示和检索中成对排列；有 Fill 配对的优先展示，没有配对的按规范顺序排列。
- 普通线性图标是全局默认样式，Fill 版通常用于选中、激活或更强视觉强调。
- 线性优先不是绝对限制，具体组件的识别习惯和既定规范可以覆盖。例如输入框中的清除或删除叉号应按组件约定使用 Fill 版本。
- 图标本身只表达图形，不自动获得交互行为；作为操作入口时必须同时满足可识别、可点击和状态反馈要求。
- 所有可点击图标必须具有合理的点击热区，并提供 Hover、Pressed、Disabled 等适用反馈；点击范围不能只等于内部图形线条。
- 点击热区随图标实际尺寸和所在组件调整。24px 是基础图标尺寸，但设计稿常使用 20px、16px 等尺寸；不得用一个固定热区值覆盖所有场景。
- 点击热区可以透明或由组件容器提供，不要求所有图标都显示可见按钮背景。
- Effect Icon 不建议用于按钮、输入框和表格操作等小尺寸、高频控件；主要用于功能入口、文件类型、状态展示，以及页面重要区块或卡片标题左侧的装饰识别。
- Effect Icon 通常不设置 Disabled；Hover 优先由图标外层区域叠加透明背景色表达，不直接破坏图标内部渐变。
- 当业务明确需要多色彩方案时，Effect Icon 可以通过颜色映射调整整体色系，但必须保持原图层关系、对比度和识别性，并由具体设计稿确认。
