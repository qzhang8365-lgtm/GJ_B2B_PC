# Dropdown audit

- 只读来源：页面 `2401:12`；组件节点 `3271:6737`、`3271:6785`、`2896:1464`、`2896:353`。
- 已确认 Single/Multiple、六个带箭头方向与无箭头模式，以及 198×240px 基准面板和 32px Item。
- 单选必须只有一个 `aria-selected=true`；Focus/Hover 不得复用 Selected 或显示勾选图标。
- Placeholder 属于 Selector Trigger，不得进入 Dropdown 的 Option 集合。
- 2026-09-09 已用本地规范补齐生成所需的状态矩阵、优先级、视口翻转/裁切、字符查找、异步状态与动态选项同步；`DRP-001` 的运行时 P1 缺口关闭。
- Figma 逐 variant 的指向箭头节点结构和变量绑定仍待只读复查，单独保留为 `DRP-002` P2；不得把本地交互契约写成已经证实的 Figma 属性。

## 2026-09-11：清理预览页残留旧 CSS（新增并关闭 DRP-003）

背景：延续 MDL-010（见 `components/modal/audit.md`）建立的方法，对 `preview/dropdown/index.html` 做同样的死代码核查——用户明确的标准原则是预览页无论静态展示区还是交互区，都应该只用标准组件基座渲染，不应该残留旧 CSS。

排查方法：同 MDL-010，解析页面全部 `<style>` 块，提取每个选择器涉及的类名，逐个类名对照页面真实 `class="..."` 属性（含 JS 动态拼接的模板字符串）确认是否被引用；复合选择器按逗号拆分成分支逐项判断，只删除全部分支都确认无引用的规则，混有真实类的复合选择器保留但裁掉死亡分支。

整条规则删除（1 条）：`.code`。

验证：脚本二次扫描确认页面残留死选择器为 0；`<style>` 块大括号计数与逐字符深度校验均通过（与 button.html 同批次校验时一并发现并排除结构性错误）；本文件删除/裁剪的选择器均逐一比对过页面真实 `class="..."` 与 `classList` 调用，确认为未引用的旧代码，属于低风险清理，未单独截图复核。

结论：DRP-003 已关闭。`preview/dropdown/index.html` 内嵌 CSS 现在只包含仍被页面实际使用的选择器。
