# Table 表格

> 从 `references/design-system-rules.md` 拆分（2026-09-08）。跨组件的颜色/字体/布局/圆角等基础规范见该文件“基础规范”章节。

- 用于大量结构化行列数据，支持选择、排序、筛选、行内操作和层级展开。第一列通常放序号、展开或 Checkbox，操作放最后一列。
- `size`：Small/Medium/Large，行高 40/48/52px；水平内边距通常 16px，Large 可 16–20px。最后一行无底部分割线，由容器收口。静态基座中 Medium 为默认档，Small 与 Large 分别使用 `gj-table-small` 和 `gj-table-large`。
- 页面生成优先复用共享 Table 组件并默认选择 Medium。Small 只用于用户明确要求的高密度场景，Large 只用于用户明确要求的疏松或复杂内容场景；不得根据页面剩余空间擅自改变密度。
- 表格外框必须使用 `Table/border`，表头列分隔线、行分隔线与子表结构线必须使用 `Table/divider`。当前 PC 模式下两者均引用 `Primitive/Neutral/N04`（`#EBEEF2`）；不得用通用 `Border/secondary`（N05）代替表格结构线。
- 尺寸是整张 Table 的原子属性：Table Header、普通 Cell、固定列 Cell 和各数据行必须使用同一尺寸变体并匹配相同行高。禁止出现 Medium 表头配 Small 数据行、固定操作列与其他列高度不同等局部混用。
- 排序、筛选、展开等表头图标必须使用 Table 组件规定的资源与状态实现。当前排序图标使用 `icf_Arrow_caret.svg`；页面不得使用 `icf_editor_sort.svg` 或其他近似图标替代。表头（包括固定末列）统一使用 `Table/head_bg`。
- 密度档只改变行高，不改变字号，也不改变单元格内组件的尺寸：三档表格的表头、正文和单元格 Tag 全部使用同一规格，Small 表格不得把 Tag 换成 Small 档。表头统一 14/22、600，正文 14/22、400。行内编辑 Input 是唯一随密度变化的单元格组件（见下条）。
- 主字段、状态和关键数据优先；短字段固定宽，长文本列弹性伸缩。超出容器允许横向滚动，末列操作固定，并以阴影提示固定列层级。
- 操作列（`gj-table-action`）表头文字与单元格内容统一居中对齐，不使用左对齐或右对齐；文字操作组、图标操作组（`gj-table-icon-actions`）在列内也按居中布局，不贴右边缘。
- 普通文本和所有表格数据均使用当前 UI 字体配对；数字列通常右对齐并统一小数位。长文本截断并提供完整信息查看方式。
- 单元格 Tag 建议 1–3 个；行内编辑复用 Input，Small/Medium 表格用 24px 控件，Large 用 32px。文字操作使用 Small Text Button，相邻操作间距使用 `Button groupGap.medium` 8px，避免无可见内边距的中文操作文字粘连；行内 Icon 操作统一 16px、间距 12px。两类间距不可混用。
- 文字操作 Hover 只使用蓝色文字高亮，不添加蓝色背景。趋势小图只用于快速识别，精确值仍需可读。
- 行选择使用 Checkbox；有选择时显示数量与批量操作，并明确全选是当前页还是全部结果。排序方向、筛选条件和清除入口必须可见且可恢复。
- 展开子表只补充当前父行信息，不重复主字段；Loading 保留表格结构，Empty 说明无数据或无结果，Error 保留查询条件并提供重试。
- 列定义是稳定契约：每列必须声明字段、标题、对齐、宽度策略与可用操作。序号、选择、展开、状态和操作列使用固定宽度；主字段至少保留一列弹性宽度。不得通过随机压缩列宽使表格“塞进”容器。
- 排序是单一数据状态，不只是图标旋转：可排序表头使用按钮并同步 `aria-sort=none|ascending|descending`；默认单列排序，切换列时清除旧列排序。若业务明确需要多列排序，必须额外显示排序优先级。
- 行 Hover、Selected、Expanded、Disabled 不得互相代替：Hover 只是指针反馈；Selected 由 Checkbox/外部选择状态驱动；Expanded 只控制子内容；Disabled 优先级最高并抑制行操作。点击行不默认等于选中，除非业务明确定义整行为单一操作热区。
- 加载时保留表头和列宽，数据区使用 Skeleton 或行内 Loading，避免布局跳动。Empty 与 Error 在表格容器内占满全列，不生成伪数据行；分页在无数据时隐藏或禁用，不得显示虚假总数。
- 滚动和固定列属于同一容器行为：只在实际溢出时显示固定列阴影，滚动到边界后消失。固定表头与固定操作列必须保持所属区域的表头/行背景，不能透出下层文字。
- 可访问性：优先使用原生 `table/th/td`；表头声明列范围，排序状态可读，Checkbox 和行内 Icon Button 有可理解名称，展开按钮同步 `aria-expanded` 与对应内容 ID。
